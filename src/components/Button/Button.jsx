import { ButtonBlue } from './Button.styled';

export const Button = ({ clickTo }) =>{

    return (
                    <ButtonBlue
                    onClick = { clickTo }
                    >Load More
                    </ButtonBlue>
    )

}